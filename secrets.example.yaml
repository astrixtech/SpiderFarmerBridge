# Copy to secrets.yaml and edit values as needed.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_ap_password: "ChangeMe1234"

# Optional only if you enable MQTT in the device YAML.
mqtt_broker: "192.168.1.10"
mqtt_username: "mqtt-user"
mqtt_password: "mqtt-pass"
